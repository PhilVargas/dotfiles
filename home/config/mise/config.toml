# Global mise tool versions. Linked to ~/.config/mise/config.toml by laptop/setup.
# `mise install` reads this; bump versions here to change them everywhere.
[tools]
ruby = "latest"
node = "lts"

[settings]
legacy_version_file = true
idiomatic_version_file_enable_tools = ["node", "ruby"]
